'use strict';

describe('yunakQuiz.measures module', function() {

  beforeEach(module('yunakQuiz.measures'));

  describe('measures controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var MeasuresCtrl = $controller('MeasuresCtrl');
      expect(MeasuresCtrl).toBeDefined();
    }));

  });
});