'use strict';

describe('yunakQuiz.assessments module', function() {

  beforeEach(module('yunakQuiz.assessments'));

  describe('assessments controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var AssessmentsCtrl = $controller('AssessmentsCtrl');
      expect(AssessmentsCtrl).toBeDefined();
    }));

  });
});